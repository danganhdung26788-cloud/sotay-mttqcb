// Runtime gate is disabled in repository source.
// GitHub Actions replaces this placeholder only in the transient workspace
// immediately before pushing a runtime-gate deployment.
const RUNTIME_GATE_TOKEN_SHA256 = '__RUNTIME_GATE_TOKEN_SHA256__';
